# Shadowpipe claims ledger

Дата среза: 2026-07-16. Этот файл — реестр проверяемых утверждений, а не список желаемых свойств. Слово «verified» всегда относится только к явно указанной области наблюдения.

## Шкала доказательности

| Tier | Что означает | Чего не доказывает |
|---|---|---|
| **E0** | Идея, вывод из аналогии или непререгистрированная гипотеза | Реализуемость и эффект на реальном censor path |
| **E1** | Unit/integration test, VM/netem, эмулятор цензора | Поведение TSPU, конкретного оператора или публичного облака |
| **E1F** | Одно авторизованное bounded field observation на одном пути/операторе/окне без репликации | Repeatability даже в том же stratum, причинность и любую экстраполяцию |
| **E2** | Повторяемое наблюдение на одном реальном пути, операторе и временном окне | Универсальность по операторам, регионам, ASN и времени |
| **E3** | Пререгистрированный paired crossover на нескольких путях/операторах/датах | Долговечность против адаптирующегося цензора |
| **E4** | Независимая репликация и продольное наблюдение | Информационно-теоретическую неотличимость или вечную устойчивость |

Статусы: `V` — подтверждено в своей области; `H` — гипотеза; `C` — противоречит данным в указанной области; `S` — scope-limited; `X` — superseded. Сочетание вроде `V/S` нормально. Исторические строки не удаляются: изменение знания оформляется новой строкой и ссылкой `supersedes`.

## 16 KiB / trigger-unit ledger

Текущая рабочая модель называется **`T0 / UNKNOWN_CONDITIONAL`**: единица и предикат срабатывания не установлены. Это не синоним «per-5-tuple». Возможные конкурирующие модели — 5-tuple, destination IP/port, prefix/ASN, классифицированный flow, операторская политика, временное правило или их конъюнкция.

| ID | Утверждение | Статус / tier | Основание и область | Фальсификатор / следующий гейт |
|---|---|---|---|---|
| `TRG-001` | Любой raw TCP flow RU→foreign-DC замерзает примерно после 15–20 KiB. | `C/S`, E1F with within-run replication | `planeb-01`: 155/155 flows завершили nominal 2 MiB targets; observed counters summed to 328.409 MB (about 313.2 MiB) versus 310 MiB nominal payload, K=1…16, один RU→NL путь и короткое окно. Это один run window, не независимая temporal replication. | Уже фальсифицировано для этого IP/порта/пути/окна. Не переносить вывод на TLS/443 или другого оператора. |
| `TRG-002` | Shadowpipe carrier на том же пути замерзает около 16 KiB. | `C/S`, E1F-mixed | `planeb-02`: до отказа прошло примерно 0.8–2.5 MB (decimal). Отказы совпали с correctness faults (`AEAD`, EOF, stall), а не с единым порогом; run не идентифицирует censor cause. | Повторить после устранения correctness faults; отделить transport error от silent degradation по pcap/TCP_INFO/тайм-аутам. |
| `TRG-003` | Chrome-TLS на проверенном RU→NL пути имеет per-flow 16 KiB ceiling. | `C/S`, E1F unsealed | Две manual Chrome-TLS сессии по 20 MiB без stall, примерно 3–11 MiB/s; raw sealed bundle не сохранён. | Повторить по датам, операторам, портам и ASN с committed raw aggregates; положительный контроль должен уметь воспроизвести stall. |
| `TRG-004a` | Repo evidence доказывает trigger unit как per-5-tuple. | `C`, E1F + logic | На проверенном scope сам trigger не возник; reset unit там не идентифицируется. Старые заметки, называющие per-5-tuple «доказанным фактом», переутверждают данные. | Любой unit claim требует сначала положительно воспроизвести trigger. |
| `TRG-004b` | В trigger-active stratum новый 5-tuple получает независимый budget. | `H`, E0 / untested | Текущие отрицательные controls эту условную модель ни подтверждают, ни фальсифицируют. | Эксперимент `001-trigger-unit`: после reproduction различить fresh tuple, destination, prefix/ASN, classifier и time models. |
| `TRG-005` | Текущая лучшая модель — `T0 / UNKNOWN_CONDITIONAL`; blanket volume rule не поддержан repo evidence. | `H`, E1F-mixed | Консервативная модель совместима с тремя scoped отрицательными наблюдениями разного качества и внешними reports; она не является E2 identification result. | Положительный пререгистрированный crossover, который устойчиво выбирает одну trigger model; независимая репликация. |
| `TRG-006` | Логический mux/H2 streams внутри одного TCP создают новые 5-tuples. | `C`, E1 + network semantics | Все logical streams делят один transport connection; fragmentation/mux не меняют TCP 5-tuple. | Только отдельные transport connections могут проверить reset-by-connection; это всё равно не доказывает reset unit. |
| `TRG-007` | TCP rotation volume guard сейчас является полезной защитой по умолчанию. | `C/S`, E1F unsealed | Guard выключен по умолчанию; target trigger не воспроизвёлся, а один manual run показал примерно три порядка throughput loss при rebuild TLS+PQ. | Положительно воспроизвести target stall, затем показать preregistered benefit с ограниченным overhead и без handshake-rate tell в sealed runs. |

Repo evidence: the public conclusions are reproduced in this ledger and
[`README.md`](../README.md). The raw operator-specific `planeb-01` and
`planeb-02` field artifacts remain private because they contain infrastructure
topology and are not required to build or test Shadowpipe.

## Carrier/topology claims

| ID | Утверждение | Статус / tier | Основание и область | Фальсификатор / следующий гейт |
|---|---|---|---|---|
| `CAR-001` | Whitelisted-cloud relay может провести трафик там, где direct foreign endpoint не проходит. | `V/S`, E1F | Vault field note 2026-07-10: один Yandex.Cloud relay прошёл на одном mobile-whitelist пути; direct control не прошёл. Это сильное единичное наблюдение, не repeatability/provider claim. | Сначала независимый повтор в том же scope для E2; затем paired AB/BA по операторам/датам/IP pools, payload floor и отрицательный cloud-IP control. |
| `CAR-002` | «Cloud whitelist relay структурно мёртв после segregation order». | `X/C`, E1F | Июльский live result опроверг blanket-вердикт для одного whitelisted Yandex.Cloud range/path. Segregation остаётся риском доступности адресов, но не доказательством невозможности. | Продольный inventory выдаваемых адресов и multi-operator field replication. |
| `CAR-003` | SNI без разрешённого destination IP достаточно для mobile whitelist. | `C/S`, E1F | На июльском scope direct endpoint с allowlisted-looking SNI не прошёл, relay destination прошёл. | Повторить с одинаковыми TLS bytes и изменением только destination topology. |
| `CAR-004` | Весь Yandex.Cloud или любой его новый адрес разрешён у всех операторов. | `H`, E0/E1F-mixed | Crowd CIDR scans и один live result дают prior, но списки operator/time-specific и неполны. | Репликация по выделенному адресу у целевого оператора в том же окне; никакой provider-wide экстраполяции. |
| `CAR-005` | CensorLess-style serverless rotation переживает RU default-deny whitelist. | `H`, E0 | CensorLess подтверждает serverless bridge, refresh/migration и cost в своей модели; не подтверждает allowlisting конкретного foreign FaaS edge в РФ. | Paired field test на целевом операторе; проход до meaningful byte floor; provider ToS review. |
| `CAR-006` | Degradation symmetry требует, чтобы treatment и real cover делили destination/path/bottleneck. | `H` grounded in primary work, E0 | Отдельный probe к другому origin измеряет чужую очередь и не идентифицирует counterfactual treatment response. | Co-routed paired experiment показывает bounded differential response при нескольких impairment levels. |

Vault provenance: operator-specific private field notes retained outside the
public repository. Their bounded conclusions are restated here without private
infrastructure details.

## Academic claims and their exact scope

| ID | Утверждение | Статус / tier | Основание и область | Нельзя из этого заключать |
|---|---|---|---|---|
| `ACA-001` | Differential degradation может разделить circumvention и cover даже при сходных пассивных traces; существует фундаментальный trade-off. | `V`, primary preprint | Sun & Shmatikov, [Differential Degradation](https://arxiv.org/abs/2409.06247), arXiv preprint. | Что российский «16 KiB» механизм имеет именно эту реализацию; что generic pacer решает трилемму. |
| `ACA-002` | Ciliate демонстрирует paired loss-response defense для конкретного WebRTC/Protozoa-like design. | `V/S`, primary | Та же работа; модификация оценивается в её threat model. | Универсальный scheduler для TLS/QUIC VPN или готовую формулу для TSPU. |
| `ACA-003` | Cross-layer RTT difference выявляет segmented proxies, где transport и application sessions заканчиваются в разных местах. | `V/S`, primary | Xue et al., [NDSS 2025 paper](https://www.ndss-symposium.org/wp-content/uploads/2025-966-paper.pdf): 80% top-5k visits содержали ≥1 detectable flow, половина — в пределах 60 packets, в исследованной постановке. | Что любой TLS-terminating single-hop tunnel уязвим тем же признаком; что одно RTT-padding делает proxy невидимым. |
| `ACA-004` | TLS-terminating single-hop, где outer transport и observable application session заканчиваются вместе, не создаёт именно исследуемый segmented-proxy RTT gap. | `H` from model boundary, E0 | Следует из определения fingerprint, но не является доказательством полной timing immunity. | Отсутствие других timing, burst, queueing или destination-side признаков. |
| `ACA-005` | Простое scheduling/delay может ослабить RTT-only detector, но не закрывает proxy detection. | `V/S`, primary cross-domain | [Beyond RTT, NDSS 2026](https://www.ndss-symposium.org/wp-content/uploads/2026-f2086-paper.pdf) показывает adversarial weakness RTT-only residential-proxy detection и добавляет другие features. | Готовую безопасную mitigation для censorship proxy; paper имеет другую основную задачу. |
| `ACA-006` | Huma реализует deferred traffic replacement; в отдельном national-Intranet mode Pung-based PIR скрывает, какой mailbox читается. | `V/S`, primary | Kamali & Barradas, [Huma, NDSS 2026](https://www.ndss-symposium.org/wp-content/uploads/2026-f328-paper.pdf), §IV-D. | Что PIR распределяет proxy endpoints или является rendezvous; low-latency bulk VPN, RU field validation или отсутствие behavioral tells вне оценённой модели. |
| `ACA-007` | Ephemeral Maybenot defenses практичны против изученных fingerprinting задач и были deployed с WireGuard/Mullvad. | `V/S`, primary | Pulls et al., [PoPETs 2026](https://petsymposium.org/popets/2026/popets-2026-0022.pdf). | Защиту от IP allowlist, differential degradation, cross-layer RTT или неизвестного TSPU classifier. |
| `ACA-008` | UPGen автоматически создаёт unidentified encrypted protocols и показал устойчивость к оценённым classifiers. | `V/S`, primary | Wails et al., [USENIX Security 2025](https://www.usenix.org/conference/usenixsecurity25/presentation/wails). | Топологическую устойчивость, whitelist bypass или неуязвимость после распространения wire samples. |
| `ACA-009` | Fountain codes подходят для короткого, unordered/lossy rendezvous; опубликован PoC over UDP и prospective encrypted-DNS design. | `V/S`, primary | Cawthon & Fifield, [FOCI 2026](https://www.petsymposium.org/foci/2026/foci-2026-0011.php). | Bulk carrier, незаметность DNS channel или работоспособность при блокировке UDP/DoH. |
| `ACA-010` | FEC не должен скрывать congestion signals; repair traffic должен учитываться вместе с CC. | `V`, primary guidance | [RFC 9265](https://www.rfc-editor.org/rfc/rfc9265.html), Informational IRTF. | Что FEC — anti-DPI defense или что RFC стандартизует конкретный tunnel scheduler. |
| `ACA-011` | CensorLess реализует serverless functions, refresher и live migration; заявляет 97% cost reduction в своей оценке. | `V/S`, primary | Kang et al., [CensorLess, PETS 2026](https://people.cs.umass.edu/~pdatta/publication/censorless/censorless.pdf). | Что конкретный FaaS prefix whitelisted в РФ или что provider не закроет fronting/relay behavior. |
| `ACA-012` | GFW использовал passive heuristics по protocol exemptions, set-bit ratio и printable ASCII для блокировки fully encrypted traffic. | `V/S`, primary | Wu et al., [USENIX Security 2023](https://www.usenix.org/conference/usenixsecurity23/presentation/wu-mingshi), измеренный GFW scope. | Что те же rules работают в РФ или arbitrary randomization даёт camouflage. |
| `ACA-013` | Encapsulated TLS handshakes могут выдавать nested proxy stacks даже при padding и нескольких слоях инкапсуляции. | `V/S`, primary | Xue et al., [USENIX Security 2024](https://www.usenix.org/conference/usenixsecurity24/presentation/xue-fingerprinting), ISP-scale оценка изученного classifier. | Что любой flow детектируется или один mux гарантирует защиту. |
| `ACA-014` | Host-based накопление результатов по нескольким flows может существенно снизить false positives flow classifier. | `V/S`, primary | Wails et al., [NDSS 2024](https://www.ndss-symposium.org/ndss-paper/on-precisely-detecting-censorship-circumvention-in-real-world-networks/), Tor-focused dataset/evaluation. | Что reported perfect recall/zero FP переносятся на Shadowpipe, другой base rate или censor deployment. |
| `ACA-015` | RFC-defined inner TCP behavior может создавать protocol-agnostic signal в TCP-over-UDP VPN traffic. | `V/S`, primary | Hanlon et al., [FOCI 2024](https://www.petsymposium.org/foci/2024/foci-2024-0016.php), real-traffic evaluation с reported 0.11% FPR. | Тот же FPR на другой сети/base rate или detection всех UDP tunnels. |
| `ACA-016` | GFW может дешифровать QUIC Initial at scale и блокировать по SNI с отдельным blocklist. | `V/S`, primary | Zohaib et al., [USENIX Security 2025](https://www.usenix.org/conference/usenixsecurity25/presentation/zohaib), GFW с апреля 2024 в изученном окне. | Что любой QUIC заблокирован, что нагрузочная weakness вечна или что RU TSPU совпадает. |
| `ACA-017` | Aggressive custom congestion control в Hysteria/TCP-Brutal отклоняется от standard TCP/QUIC congestion response и может дать simple impairment-response classifier. | `V/S`, primary | Wang et al., [FOCI 2025](https://www.petsymposium.org/foci/2025/foci-2025-0001.php), два изученных tools и threshold classifiers. | Что любой custom controller detectable или standard CC даёт censorship resistance. |
| `ACA-018` | Российские TSPU широко перешли от blanket QUIC block к SNI-dependent QUIC censorship в 2022–2023 timeline исследования. | `V/S`, primary | Heitmann et al., [FOCI 2026](https://www.petsymposium.org/foci/2026/foci-2026-0010.php). | Что каждый оператор/регион применяет тот же rule сейчас; что весь QUIC или любой SNI заблокирован. |
| `ACA-019` | Obscura реализует WebRTC media-stream encapsulation, reliability layer и ephemeral browser/Pion proxies в cost-effective-censor threat model. | `V/S`, primary + functional artifact | Vilalonga et al., [PoPETs 2026](https://petsymposium.org/popets/2026/popets-2026-0030.php). | Неидентифицируемость против глобальной корреляции, RU field validation или пригодность как universal bulk TUN carrier. |
| `ACA-020` | Dual-role server/client behavior даёт relay-detection signal даже при encrypted payload. | `V/S`, primary | Almutairi et al., [FOCI 2026](https://www.petsymposium.org/foci/2026/foci-2026-0008.php): 17 TB WIDE dataset, reported 23% relay recall and 0.18% FPR in that evaluation. | Тот же FPR при другом base rate/traffic mix; detection всех relays или причинность для TSPU deployment. |
| `ACA-021` | Ephemeral browser/mobile proxy distribution can change the censor–distributor game relative to static proxy models. | `V/S`, primary simulation | Fares et al., [FOCI 2026](https://www.petsymposium.org/foci/2026/foci-2026-0003.php), simulated NAT/ephemeral and traffic-analysis-aware strategies. | Готовый optimal distributor, immunity to enumeration or a field result for ZATMENIE. |
| `ACA-022` | Evidence-based automatic circumvention-technique selection уже реализована для DNS. | `V/S`, primary | Lange et al., [DPYProxy-DNS, FOCI 2026](https://www.petsymposium.org/foci/2026/foci-2026-0001.php): boot-time selection and caching of a working resolver/mode combination in China/Iran experiments. | Новизну generic adaptive selection; bulk-carrier topology, continuous causal promotion, RU result или signed evidence. |
| `ACA-023` | NinjaDoH реализует moving-target DoH с hyperscaler-IP rotation, IPNS dissemination и overlap старого/нового endpoint. | `V/S`, primary workshop prototype | Seidenberger et al., [MADWeb 2026](https://www.ndss-symposium.org/wp-content/uploads/madweb2026-6.pdf). | Устойчивость к strict IP allowlist, adversarial rendezvous compromise, bulk VPN или rollback-safe causal selector. |
| `ACA-024` | Предсказуемые NAT/routing/tunnel responses могут дать blind in-path oracle для inference и TCP disruption без расшифровки VPN payload. | `V/S`, primary experience + re-test | Tolley et al., [FOCI 2026](https://www.petsymposium.org/foci/2026/foci-2026-0007.php), re-test на Android 16 и нескольких VPN families. | Универсальную эксплуатацию каждого OS/config или deployment этим конкретным цензором. |
| `ACA-025` | Assemblage переносит небольшие Raptor-coded сообщения через AI-generated images на публичных платформах. | `V/S`, primary workshop prototype | Jois et al., [FOCI 2026](https://www.petsymposium.org/foci/2026/foci-2026-0005.php), восемь platform tests. | Bulk transport, bootstrap без pre-shared state, undetectability или censor-field validation. |
| `ACA-026` | Популярные measurement lists могут пропустить большую долю доменов из vendor-side censorship corpus. | `V/S`, primary scoped measurement | Sheffey et al., [Geedge Cases, FOCI 2026](https://www.petsymposium.org/foci/2026/foci-2026-0006.php): в их measured GNL set 298,955 censored domains отсутствовали в обеих сравниваемых lists. | Что каждый leaked domain заблокирован, что TLS EOF однозначно означает censorship или что corpus переносится в РФ. |
| `ACA-027` | Android VPN ecosystem содержит реальные классы cleartext bootstrap/config, DNS/traffic leaks и weak tunnel configuration. | `V/S`, primary ecosystem audit | Wang et al., [MVPNalyzer, NDSS 2026](https://www.ndss-symposium.org/ndss-paper/mvpnalyzer-an-investigative-framework-for-auditing-the-security-privacy-of-mobile-vpns/), 281 apps. | Что эти дефекты есть в Shadowpipe или что app-store sample описывает каждое VPN-приложение. |
| `ACA-028` | Номинально разные relays могут иметь общий transit-AS chokepoint; bandwidth-weighted concentration важна для correlation risk. | `V/S`, primary Tor measurement | Wang & Cho, [FOCI 2026](https://www.petsymposium.org/foci/2026/foci-2026-0014.php), longitudinal Tor AS-Hegemony study. | Те же AS paths/concentration у CCP или фактическую censor correlation на них. |
| `ACA-029` | Во время shutdown/recovery protocol failures могут меняться во времени и отражать infrastructure drift, а не один censor predicate. | `V/S`, primary scoped observation | [Insights into an Iranian Internet Shutdown, FOCI 2026](https://www.petsymposium.org/foci/2026/foci-2026-0016.php). | RU behavior, причинность конкретного механизма или что любой UDP failure является censorship. |
| `ACA-030` | FEP security требует моделировать datastream/datagram metadata protection, error response и minimum-message distinguishers, а не только отсутствие plaintext magic. | `V/S`, primary formalization | Fenske & Johnson, [Bytes to Schlep? Use a FEP](https://arxiv.org/abs/2405.13310), задаёт FEP notions/constructions и показывает новые distinguishers. | Что Shadowpipe/REALITY удовлетворяет этим определениям; что random-looking bytes дают blocking resistance или скрывают timing/topology. |
| `ACA-031` | Реальные FEP implementations терпели decryption-oracle, non-uniform representation и replay/malleability distinguishing failures. | `V/S`, primary retrospective | Fifield, [Comments on certain past cryptographic flaws](https://eprint.iacr.org/2023/1362.pdf), case studies Shadowsocks, obfs4 и VMess. | Что перечисленные исправленные flaws присутствуют здесь; что криптографический review заменяет endpoint/topology threat model. |
| `ACA-032` | OKE формально объединяет key security/authentication с random-looking transcript и active-probing resistance; опубликованы obfs4 analysis и ML-KEM-oriented Kemeleon construction. | `V/S`, primary cryptographic work | Günther, Stebila & Veitch, [Obfuscated Key Exchange](https://eprint.iacr.org/2024/1086.pdf), CCS 2024. | Что current REALITY + protocol-v3 является OKE/pq-obfs/Kemeleon, наследует proof или скрывает all traffic features. |
| `ACA-033` | Signed monotonic versions сами по себе не дают global split-view detection или compromised-authority containment; key transparency добавляет monitoring/fork-consistency assumptions. | `V/S`, primary standards/preprint | [IETF KEYTRANS architecture draft-09](https://datatracker.ietf.org/doc/html/draft-ietf-keytrans-architecture-09) и Brorsson et al., [Consistency-or-Die](https://eprint.iacr.org/2024/879.pdf). | Что Shadowpipe реализует transparency log, gossip, auditors, witnesses или CoD; что эти mechanisms автоматически подходят endpoint policy. |
| `ACA-034` | NetShaper даёт configurable differential-privacy traffic-shaping guarantee для network side channels с bandwidth/latency trade-off. | `V/S`, primary systems work | Sabzi et al., [NetShaper, USENIX Security 2024](https://www.usenix.org/conference/usenixsecurity24/presentation/sabzi). | Censorship resistance, degradation symmetry, path availability или пригодность как готовый Shadowpipe pacer. |
| `ACA-035` | Migration, concurrent address attempts and multipath transport/path management are established prior art. | `V/S`, standards + primary system | [RFC 9000](https://www.rfc-editor.org/rfc/rfc9000.html), [RFC 8684](https://www.rfc-editor.org/rfc/rfc8684.html), [RFC 8305](https://www.rfc-editor.org/rfc/rfc8305.html), [QUIC multipath draft-21](https://datatracker.ietf.org/doc/draft-ietf-quic-multipath/) и [QUICstep](https://petsymposium.org/popets/2026/popets-2026-0014.php). | Новизну generic failover/multipath/path selection; draft не задаёт address discovery или scheduler, а QUICstep не является general causal policy loop. |
| `ACA-036` | Address family, resolver, provider/CDN replica and peering path могут менять censorship/measurement outcome; country/provider label не является path-equivalence proof. | `V/S`, primary preprints + path-aware guidance | [ProtoScan](https://arxiv.org/abs/2508.07194), [Not All Roads Lead to Rome](https://arxiv.org/abs/2605.30692), [RFC 9049](https://www.rfc-editor.org/rfc/rfc9049.html) и [RFC 9217](https://www.rfc-editor.org/rfc/rfc9217.html). | Что IPv6 лучше IPv4, что inferred paths независимы, или что current CCP authenticates trustworthy path properties. |

## Architecture/novelty claims

| ID | Утверждение | Статус / tier | Основание и область | Фальсификатор / следующий гейт |
|---|---|---|---|---|
| `ARC-001` | Гипотеза: joint heterogeneous carrier topology + preregistered causal promotion + fail-closed signed policy может быть new systems contribution. | `H`, E0 | [Related-work matrix v5](related-work-matrix-2026.md) дополнительно исключает novelty generic multipath/failover, FEP/OKey exchange, traffic shaping и key-transparency primitives в проверенном bounded corpus, но не является завершённым systematic review или независимой novelty assessment. | Выполнить воспроизводимый search и independent review; проверить `F-NOV-1`…`F-NOV-5` из matrix. До этого запрещены «first» и «не существует» claims. |
| `ARC-002` | Текущий bytes plane использует staged custom v3: mutual fixed-width PSK access gate, затем ML-KEM-768 + ephemeral X25519 в HKDF-SHA-256, encrypted Ed25519+PSK Finished и ChaCha20-Poly1305 application frames. | `V/S`, implementation inspection + adversarial E1 tests | `client_auth.rs`, `crypto/mod.rs`, `session.rs`, `e2e_auth.rs`; серверный access proof предшествует client proof, а ML-KEM key publication/KEM work — успешному client access proof. Статический ML-KEM key также аутентифицируется signed-policy fingerprint pin. | Это не TLS/Noise conformance, не «PQ AEAD», не доказанный hybrid/PSK combiner и не независимый cryptographic audit. Ed25519 остаётся classical; KAT фиксирует реализацию, но не доказывает security. |
| `ARC-003` | v3 аутентифицирует совпадение inner framing class `raw`/`h2`, но не уникальную identity outer transport. | `V/S`, implementation inspection + mismatch E1 test | `CamouflageMode` входит в access MAC и `H0`; server сравнивает authenticated claim с наблюдаемым framing до decapsulation. Direct TCP, TLS, QUIC и REALITY все дают `raw`, поэтому cross-transport replay между ними этим полем не разделяется. Production daemon отдельно допускает только REALITY; остальные carriers — explicit no-TUN lab. | Называть это полным carrier/channel binding нельзя. Для такого claim нужен отдельный transcript-bound exporter/transport ID для каждого carrier и adversarial cross-carrier matrix. |
| `ARC-004` | Policy-v2 rotation обеспечивает bounded rollout continuity, но не containment compromised Active online signer. | `V/S`, implementation inspection + adversarial E1 tests | Exact sorted `service_id` set immutable after genesis; candidate signer Active-only; key/pin addition и `Active -> Retiring` требуют complete `86400 s` usable overlap; revoked keys removable in later contiguous keyset. Но Active signer может добавить attacker pin рядом со старым pin. | Containment claim требует отдельно root/threshold-authorized service registry, recovery semantics и transparency/witnessing; это Phase-4 `E0`. |
| `ARC-005` | Durable REALITY exact-replay rejection является same-host fixed-state guarantee, не distributed admission protocol. | `V/S`, implementation + adversarial E1 tests | HMAC/static-key-bound file `96 + 16,384*96 = 1,572,960 B`; keyed SID digest и `fdatasync` до accepted flight; corruption/I/O/full fail forward. | Cold-start store loss с тем же key забывает live admissions; replicas require linearizable reserve-before-flight (`SETNX + TTL`-equivalent), and valid short-id holder retains bounded sync/saturation DoS. |
| `ARC-006` | Phase-4 research delta, если он выживет systematic review, может быть только authenticated evidence provenance + preregistered causal carrier promotion + fail-closed authority. | `H`, E0 / not implemented | TUF/KEYTRANS/CoD, FEP/OKey exchange, NetShaper, QUICstep, QUIC/MPTCP/multipath/Happy-Eyeballs и path-aware RFCs уже покрывают individual mechanisms. | Любой prior system с эквивалентным enforceable loop сужает или фальсифицирует claim; до runtime evidence verifier, E3 result и independent review novelty остаётся open. |

## Operational invariants (не гипотезы)

| ID | Инвариант | Статус | Enforcement |
|---|---|---|---|
| `OPS-001` | Живой macOS sing-box нельзя останавливать, перезапускать, перенастраивать или обходить. | `REQUIRED` | Host is observer/build coordinator plus narrowly scoped unprivileged `127.0.0.1`/`::1` correctness smoke; no route/DNS/PF/TUN/service mutation or non-loopback bind. |
| `OPS-002` | Все privileged/persistent network-state mutations, impairment и destructive tests выполняются только внутри disposable VM/netns. | `REQUIRED` | OrbStack Arch guest + scoped veth/qdisc/nft; veth briefly exists in the guest root namespace while being moved, but management routes/qdisc are out of scope. Ephemeral host loopback sockets are not state mutation. Parallels Windows только portability/socket smoke; no host `sudo`. |
| `OPS-003` | Новые publishable experiment bundles не содержат credentials, private keys, subscription URLs, production endpoint inventory или raw session traces. | `REQUIRED` | `.invalid` names, access-controlled raw evidence, explicit reducer/redaction before export. Исторические vault/review notes могут содержать sensitive inventory и не являются publishable bundles. |
| `OPS-004` | VM/netem result доказывает только корректность harness/implementation. | `REQUIRED` | Одно field observation получает не выше E1F; repeatability/causality требуют отдельно авторизованные E2/E3 runs. |
| `OPS-005` | Каждый current client mode обязан пинить server ML-KEM fingerprint; unpinned lab exception отсутствует. | `REQUIRED / ENFORCED IN CURRENT RUST CLIENT` | `--server-fp`/URI `fp=` разбирается до reservation trace, DNS, socket и TUN; missing/malformed pin fail closed. `ClientConfig` хранит non-optional `[u8; 32]`, mismatch проверяется constant-time до encapsulation/application bytes. CLI/unit tests отвергают missing pin и mixed pinned/unpinned URI pool. Trusted distribution/rotation остаётся в `OPS-006`; это не делает весь продукт production-ready. |
| `OPS-006` | Signed endpoint policy имеет canonical bytes, immutable enrolled service authority, key/policy epochs, expiry и persistent anti-rollback floors; invalid/expired state не создаёт endpoint authority. | `REQUIRED / IMPLEMENTED; V/S, E1 COMPONENT` | Root-signed keyset v1 и online-signed endpoint-policy v2 используют строгий fixed `COSE_Sign1` over deterministic CBOR и Ed25519. Exact sorted `service_id` set immutable after genesis; candidate signer must be Active; new key/pin and `Active -> Retiring` require complete `86400 s` usable overlap; revoked keys may be removed in a later contiguous keyset. Проверяются canonical profile, predecessor chain, rollback/fork/gap and clock floor; state публикуется crash-safely. Overlap is continuity, not containment: compromised Active signer can add attacker pin. Это не TUF conformance, threshold service registry, transparency, independent audit, fleet enrollment или causal activation. |
| `OPS-007` | Раздельно собранные release client/server имеют один явно записанный `SHADOWPIPE_MAGIC`; он не секрет и не аутентификатор. | `REQUIRED / ENFORCED AT RELEASE BUILD` | Build script пересобирается при смене env, rejects невалидный u32 и отказывается собирать `PROFILE=release` без explicit `SHADOWPIPE_MAGIC`. Debug/test без env сохраняет случайный development default. Artifact pairing/manifest всё ещё обязан записывать значение; server fingerprint pin и credentials, а не magic, остаются trust anchors. |
| `OPS-008` | Full-tunnel `--auto-route` не может запускаться без fail-closed routing/leak controls. | `REQUIRED / ENFORCED; V/S, E1 LINUX IPV4 VM` | Current CLI принимает `--auto-route` только на Linux вместе с `--tunnel --kill-switch --dns`; invalid configuration aborts до DNS/socket/TUN/host mutation. Install order — kill-switch, exact carrier/SSH bypass, split-default, DNS guard. Current-source run [`20260716T123535Z-91294-70zWb7`](../tests/tun/results/20260716T123535Z-91294-70zWb7/RESULT.md) дал valid test+cleanup+host-safety в disposable OrbStack IPv4/netns: production REALITY + v3 auth, ICMP/TCP/UDP/DNS/64 MiB, strict non-carrier/missing-auth/restart captures `0/0/0`, marker `28/28/0`, sink `5124/5125/0`, carrier cut/recovery upper bound `8 s`, explicit release и 573/573 checksum entries. Planted foreign persistent named TUN collided before mutation and remained exact unchanged. `field_evidence=false`; IPv6, native Windows/macOS TUN и production/field остаются separate gates. |
| `OPS-009` | Live DNS не может расширить signed endpoint authority; host state и dial snapshot меняются согласованно. | `REQUIRED / IMPLEMENTED; V/S, E1 COMPONENT` | Policy v2 подписывает независимые `locator_name` и REALITY `sni`; оба входят в group/authority digests. DNS выбирает только subset exact signed IPv4 set. Addition выполняется `allow tuple -> exact /32 route -> publish`; retirement сначала depublish, затем после final socket lease `deny -> route remove`; shared resources refcounted, expiry/model-commit races компенсируют host prefix. Не доказаны IPv6, arbitrary resolver/path behavior или censor availability. |
| `OPS-010` | Carrier outage не открывает underlay DNS bootstrap channel. | `REQUIRED / IMPLEMENTED; V/S, E1 COMPONENT` | Во время reconnect backoff DNS не вызывается. После исчерпания current DNS-selected subset один раз на failure epoch addition-only transaction восстанавливает полный private signed IPv4 authority; caller/DNS не может подать новый адрес. После carrier success tunneled DNS снова может сузить subset. Это availability fallback, не DNS-circumvention или field result. |
| `OPS-011` | Crash recovery не мутирует Linux host state без durable ownership evidence. | `REQUIRED / IMPLEMENTED; V/S, E1 LINUX NAMESPACE VM` | Every Linux TUN берёт global lease; startup recovery идёт до policy/DNS/socket/TUN. WAL требует boot ID, PID start ticks, netns и mntns; bounded anchored schema v3 records the exact eight-resource vocabulary before mutation. Current-source run [`20260716T124109Z-93828`](../tests/host-recovery/results/20260716T124109Z-93828/FINAL-RESULT.md) прошёл 29/29 fresh-namespace scenarios и 1443/1443 checksum entries: `SIGKILL` cuts at Planned/apply/DNS-staged/partial-ack/Active and before/after each recovery step, all-resource preflight, durable Conflict and firewall-last convergence. Scope — same boot only; no kernel reboot, torn-write or power-loss claim. |
| `OPS-012` | Recovery не удаляет TUN по имени/ifindex, а explicit named client/server не присоединяются к existing TUN. | `REQUIRED / IMPLEMENTED; V/S, E1 LINUX NAMESPACE VM + COMPONENT` | Production TUN non-persistent; normal close proves absence. Crash recovery never sends `RTM_DELLINK`; present/reused/foreign interface is Conflict and retains protection. Both explicit named client and server creation use `IFF_TUN_EXCL`; unnamed client path uses kernel allocation. Full-TUN run planted a persistent empty-alias foreign `sptunc`: client failed with exclusive collision, zero packets/routes/firewall/WAL and exact unchanged fixture. Cross-kernel/native-OS privileged matrices не закрыты. |
| `OPS-013` | Route/firewall helper failure имеет bounded parent-side liveness and cannot be accepted from empty stderr. | `REQUIRED / IMPLEMENTED; V/S, E1 COMPONENT` | Helpers run with deadlines and output caps, nonblocking pipe drains, fresh process group, group/direct-child kill and guaranteed reap on timeout/overflow. Route success requires exact pre/post state. Helpers remain trusted executables; deliberate process-group escape is outside this userspace bound and needs OS confinement. |
| `OPS-014` | Scoped integrated privileged PASS не равен production/field PASS. | `REQUIRED / ENFORCED IN CLAIM LANGUAGE` | Current-source bundles: Linux ARM64 portability [`20260716T122834Z-linux-arm64-current`](../tests/portability/results/20260716T122834Z-linux-arm64-current/RESULT.md) 342 entries; full-TUN IPv4/netns [`20260716T123535Z-91294-70zWb7`](../tests/tun/results/20260716T123535Z-91294-70zWb7/RESULT.md) 573; same-boot Phase-3 [`20260716T124109Z-93828`](../tests/host-recovery/results/20260716T124109Z-93828/FINAL-RESULT.md) 1443/29 scenarios; reboot lockdown [`20260716T124706Z-34564-reboot`](../tests/lockdown/results/20260716T124706Z-34564-reboot/RESULT.md) 650; Windows ARM64 no-TUN [`20260716T125113Z-36840-dd0c2571`](../tests/windows/results/20260716T125113Z-36840-dd0c2571/RESULT.md) 891. All say `field_evidence=false`; no combination proves IPv6, native Windows/macOS TUN, hostile-operator availability, fleet rollout or continuous Mac non-mutation. |
| `OPS-015` | REALITY replay store сохраняет exact-replay rejection через same-host restart и ограничивает state/work, но не гарантирует carrier availability или distributed consistency. | `REQUIRED / IMPLEMENTED WITH RESIDUAL DoS` | Before bind, exclusive lease validates HMAC/static-key-bound `96 + 16,384*96 = 1,572,960 B` file. Keyed SID digest with absolute `valid_until=token_time+window` is written and `fdatasync`-ed before accepted flight; replay/corruption/full/lock/I/O fail forward, with at most 64 expiries removed per admission. Valid short-id holder can force sync/saturation. Cold-start state loss with same key forgets live admissions; replicas sharing a key require linearizable reserve-before-flight (`SETNX + TTL`-equivalent), not eventual consistency. |
| `OPS-016` | После reboot Linux host не должен получить широкую L3 OUTPUT connectivity до восстановления fail-closed barrier. | `REQUIRED / IMPLEMENTED; V/S, E1 DISPOSABLE REBOOT VM` | Run [`20260716T124706Z-34564-reboot`](../tests/lockdown/results/20260716T124706Z-34564-reboot/RESULT.md) записал разные boot IDs, strict WAL/PID-1 namespace binding, restore completion before networkd, loopback success, non-loopback IPv4 denial, explicit release и 650/650 checksum entries. Это early-userspace local OUTPUT proof без paired client/server tunnel; нет production/initrd/L2/AF_PACKET/FORWARD/container-netns claim. |
| `OPS-017` | Host-safety evidence обязано явно отделять наблюдаемое от ненаблюдаемого. | `REQUIRED / ENFORCED IN CURRENT BUNDLES` | Mac routes, DNS, static PF files и exact sing-box identity сравнивались только в bounded before/after points, не continuous monitor. Loaded PF runtime был недоступен unprivileged collector (`pf_runtime_observed=false`). OrbStack guest operations bound opaque IDs; из-за observed OrbStack 2.2.1 delete-by-ID panic delete-by-name допускается только после fresh name→bound-ID equality и последующего ID/name absence proof. Unrelated same-host OrbStack operators остаются outside trust boundary. |
| `OPS-018` | Orderly expired policy hash нельзя воскресить только rollback-ом wall clock после durable expiry checkpoint. | `REQUIRED / IMPLEMENTED; V/S, E1 COMPONENT` | `<policy-state>.expired-v1` is exact 132-byte `SPPOLEX1` tombstone with root ID, policy hash, epoch/sequence, signed expiry and checksum; write reloads exact anchor under lock, preventing stale runtime from tombstoning a successor. Same-storage snapshot/rollback can revert state+tombstone together; crash/power loss before checkpoint and trusted time remain open. |
| `OPS-019` | Native Linux ARM64 current-source evidence must bind an exact frozen source snapshot and disclose post-snapshot drift. | `REQUIRED / V/S, E1 PORTABILITY` | Run [`20260716T122834Z-linux-arm64-current`](../tests/portability/results/20260716T122834Z-linux-arm64-current/RESULT.md) sealed 342 checksum entries and captured 187 source files under manifest `fd5ebffc5b820ec8ac037aa3e9fea154c62576d7a276fa923168e5f4b4a84b95` (165 non-Markdown + 22 Markdown). After the evidence capture, ten Markdown files changed during documentation finalization: current tree remains 165/165 for non-Markdown and 177/187 total against the frozen manifest. This does not retroactively alter the captured build/test snapshot and is not privileged-network evidence. |
| `OPS-020` | Native Windows ARM64 H2 no-TUN evidence is artifact-bound and must not be presented as Wintun/full-VPN support. | `REQUIRED / V/S, E1 PRIVATE-VM PORTABILITY` | Run [`20260716T125113Z-36840-dd0c2571`](../tests/windows/results/20260716T125113Z-36840-dd0c2571/RESULT.md) sealed 891 entries; missing pin opened 0 connections, unenrolled auth got no echo, enrolled path echoed exact nonce + 1,048,576 bytes. PE is 5,072,384 bytes, SHA-256 `2734e79f98866910aa8e0386af4ff630191b0a72fd1945177f078cb69d500bad`. No TUN/firewall/adapter mutation or production claim. |
| `OPS-021` | sing-box, sing-tun and Xray remain clean-room platform references, not a replacement architecture or implementation dependency. | `REQUIRED / DESIGN INPUT, NOT IMPLEMENTATION EVIDENCE` | The pinned [`upstream platform audit`](upstream-platform-audit-2026-07-16.md) separates immutable Shadowpipe protocol/policy/carrier/WAL invariants from independently reimplementable OS mechanisms, records GPL-3.0-or-later/MPL-2.0 boundaries and ranks Linux/macOS/Windows work. Inner IPv6 may be deferred, but the default Alpha mode is explicit IPv6 block; outer-only IPv6/NAT64 precedes full inner IPv6. No upstream source, prose, checkout or build input may enter this tree without a separately approved licensing decision. |
| `OPS-022` | IPv4-only Alpha must express an explicit IPv6 policy and may not silently accept an unavailable mode. | `REQUIRED / PARTIALLY IMPLEMENTED; V/S, E1 COMPONENT` | Client CLI exposes `--ipv6-mode {block,outer-only,tunnel}` with default `block`. `outer-only` and `tunnel` are rejected before credential provisioning, policy/DNS, socket, TUN or host-state operations. Current Linux full-TUN entrypoints explicitly request `block`; its kill-switch installs loopback-only accept followed by non-loopback IPv6 drop before IPv4 route publication. This is not inner IPv6 support, outer IPv6/NAT64 support, or native macOS/Windows leak evidence. |

## Правило обновления

Новый результат добавляет: experiment ID, git SHA, manifest hash, UTC window, topology class, operator/path stratum, payload floor, fixed observation horizon, complete/stall/error taxonomy и ссылку на raw aggregate. No-target-event на fixed horizon отмечается как right-censored; payload completion и implementation failures учитываются как отдельные terminal/competing causes, а не автоматическое non-informative censoring. Если эксперимент не может отличить censor action от implementation fault, его verdict — `INCONCLUSIVE`, а не `BLOCKED` и не `PASSED`.
