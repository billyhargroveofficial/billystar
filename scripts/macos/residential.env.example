# Auto-managed by scripts/macos/residential.sh start.
# The REALITY URI is intentionally not stored in this user-readable env file;
# root publishes it as /private/etc/shadowpipe/client-endpoint.uri (0600).
